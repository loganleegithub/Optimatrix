# Task — B3 completed Session review

**Status:** ACTIVE

**Task kind:** VALIDATION_ONLY

**Target maturity stage:** `B3_ATOMIC_PUBLIC_SHADOW`

**Runtime implementation:** FORBIDDEN

**Live commands:** the already deployed launchd label `com.optimatrix.b3-public-shadow` may continue
its exact current public Shadow command and append later-Session facts to the exact v2 root under
`KeepAlive`; this task may not start, stop, restart, replace, signal, or reconfigure it. One bounded
read-only process may inspect the completed Session's Ledger, WindowOutcomes, settlement, future
index path, runtime audit, and loopback Workbench. It may make no market call and write no durable
root member.

The preserved runtime and review use Policy identity
`sha256:b282de121a676da13c16d73d41ac19c4b5a17366bd89b7036ef07d0bd05e9888` and exact root
`/Users/logan/Library/Application Support/Optimatrix/b3-natural-forward-chain-v2`.

**Owning authority/contract:** `docs/authority/PRODUCT_CONSTITUTION.md`,
`docs/authority/CURRENT_STAGE.md`, `docs/authority/SYSTEM_ARCHITECTURE.md`,
`docs/contracts/BTC_0DTE_TWO_SIDED_SHORT_VOL.md`, and
`docs/contracts/CASE_POSITION_OUTCOME.md`

No placeholder remains. Stage links this file as the only active non-template task.

## Closure

**Given:** the manifest-enrolled Session `2026-08-14T08:00:00Z/2026-08-15T08:00:00Z` is complete;
its root contains `81` DecisionRecords and `81` WindowOutcomes for every Window encountered from
the first enrolled `11:45 UTC` Window onward, one continuous public future-index-path capture, and
official delivery price `63013.74 USD`

**When:** one read-only review reconciles Session coverage, Decision and blocker distributions by
phase, DataHealth and environment boundaries, legal/evaluable/eligible structure reachability,
actual future-path and short-strike risk, the frozen `7%`-only marginal, runtime failures and
recovery, settlement, and Workbench projection

**Then:** the completed Session has one evidence-bounded adjudication of system correctness,
observed Policy reachability, local gate responsibility, missed-risk examples, data limitations,
overdesign or defensive-development findings, and exactly one smallest next validation or change;
no market, Policy, Candidate, Position, Outcome, or root fact is created or relabelled

**Affected identity and population:** exactly the `81` current-Policy DecisionWindow identities in
Session `2026-08-15T08:00:00Z` that have authoritative DecisionRecords; the `15` pre-enrollment
calendar Windows remain measured absence, current-Session appends are excluded, and structures do
not multiply the Window denominator

**Baseline and denominator:** `96` pre-registered calendar Windows, `15` absent before enrollment,
`81/81` enrolled DecisionRecords, `81/81` matching WindowOutcomes, `0` Candidate, `0` TradeCase,
and `0` Position in the completed Session

**Primary blocker and expected delta:** `COMPLETED_SESSION_BEHAVIOR_NOT_REVIEWED` becomes one direct
review with an exact next blocker; one Session remains insufficient for Policy qualification or
Edge

**Known-at and DataHealth boundary:** only immutable Session-owned observations, outcomes, runtime
audit events, official settlement, and their recorded known-at boundaries may enter. Missing
pre-enrollment Windows remain absent; `UNKNOWN`, discontinuity, and unavailable route or execution
facts retain their exact meaning

## Effects and scope

**Risk allocation effect:** NONE

**ObservationLedger / CaseJournal effect and consumer:** read-only access to the exact v2 root; the
existing runtime remains the sole writer. No CaseJournal fact is expected because the Session has no
Candidate, and the review writes no record, report, copy, backfill, or enrichment

**Legacy-data effect:** NONE; v1 remains excluded

**Permission effect:** preserve only the exact existing public launchd deployment and add no market,
private, order, capital, Policy-promotion, or remote-deployment permission

**Files and behavior in scope:** completed-Session files in the v2 root, read-only Workbench,
this task, matching Stage snapshot, and direct Authority structural checks

**Out of scope:** current-Session interpretation, waiting for a Candidate, runtime or Policy
implementation, changing `7%`, `$10`, sizing or ranking, new Policy identity, Challenger, durable
review artifact, synthetic or Monte Carlo path, private fact, Combo or fill claim, order, capital,
Policy qualification, Edge claim, and B4

**Complexity added / deleted:** add no abstraction, dependency, schema, retry, model, or durable
file; delete only the completed fixed-window outcome task

## Verification and closure

**Cheapest falsification:** prove `81/81` DecisionRecord-to-WindowOutcome identity alignment and
separate the completed Session from the seven or more later-Session records before interpreting any
count

**Repository gate:** focused Authority tests; rerun `make check` only if tracked source or structural
tests change

**External evidence:** public Session facts only; execution, private account truth, repeated-Session
Policy value, and Edge remain `UNVERIFIED`

Close only after directly observing the declared review delta. Replace Stage with the post-task
snapshot and remove this file; do not append completion history.
