# Optimatrix Current Stage

**Status:** ACTIVE PERMISSION AUTHORITY

**Current permission boundary:** `PUBLIC_SHADOW`

**Implemented runtime capability:** `NONE` — clean authority-aligned baseline

**Production Short Vol Radar:** `NOT_ESTABLISHED`

**Sole authorized next product-capability closure:** `SHORT_VOL_RADAR_ESTABLISHMENT`

## Authority

This document grants permission under
[`PRODUCT_CONSTITUTION.md`](PRODUCT_CONSTITUTION.md). It does not own product definitions,
architecture, detector parameters, or evidence mechanics. Code presence, green tests, old
receipts, run duration, or roadmap order grant no stage.

The current boundary authorizes one implementation task inside the existing modular
monolith: establish the first production-public Short Vol Radar defined by
[`SHORT_VOL_RADAR.md`](../contracts/SHORT_VOL_RADAR.md). Candidate, Shadow admission, executed
entry, Position runtime, Outcome cohort, private/account data, orders, fills, capital,
qualification, Challenger automation, promotion, and execution remain unauthorized.

No product implementation task is active in this clean baseline. Before implementation begins,
one task must freeze the exact `SHORT_VOL_RICHNESS_RADAR_POLICY`; it may not change that Policy
after observing production acceptance evidence.

## Clean baseline

The legacy bounded collector, replay path, fixed-horizon Decision logic, elapsed-time Shadow and
Outcome logic, receipts, bundles, and product commands have been physically removed. No
compatibility layer or runnable product path remains.

Known-at inputs, explicit missingness and continuity, strictly future facts, and visible-quote
economics remain product invariants in authority. They are not claims of an implemented runtime
capability.

## Root blocker

The production path has not proved one natural instance of:

```text
live BTC 0–3DTE state
→ frozen Short Vol anomaly
→ same-scope target-size atomic defined-risk credit structure
→ minimal SHORT_VOL_RADAR_HIT
```

Until that exists, Candidate and Outcome work has no established Radar input. A legged
bid/ask reference is useful diagnostic information but has execution risk between legs and does
not close this blocker.

## Authorized closure

`SHORT_VOL_RADAR_ESTABLISHMENT` may implement only:

- one continuous public WebSocket-fed, bounded in-memory BTC-USDC option-chain monitor;
- actual timestamp-based `0 < TTE <= 72 hours` membership and trusted Deribit-time handling;
- snapshot/increment book continuity, freshness, known empty/unexecutable versus `UNKNOWN`, and
  affected-scope recovery;
- one pre-frozen pointwise executable-IV richness hypothesis with exact units, trigger,
  expiry/option-type episode scope, short-leg mapping, confirmation, persistence, clear, and
  re-arm;
- same-expiry 1:1 OTM call or put verticals whose short leg is in the triggered set and whose
  target-size atomic combo bid has positive net credit after official fees;
- one hit snapshot containing the complete triggered-short-leg set and every qualifying atomic
  structure within the declared usable combo-book scope at the first hit state in stable canonical
  order;
- minimal coverage/gap metrics and direct health metrics necessary to interpret no-hit versus
  blindness.

Normal full-chain facts, no-hit rows, theoretical structures, and component-leg references are
not durable product evidence. A component-leg reference cannot create `RADAR_HIT`. No Candidate,
Shadow Entry, Position action, or Outcome is emitted.

## Acceptance boundary

The implementation closure is accepted only when:

1. direct deterministic tests cover the Monitor, detector state machine, episode/short-leg
   mapping, authorized vertical, atomic economics, known unexecutable versus `UNKNOWN`, and
   unchanged-state suppression defined by the Short Vol contract;
2. one continuously running production-public process naturally emits at least one
   `RADAR_HIT` containing a target-size atomic combo quote;
3. a pure calculation reproduces every triggered short leg's frozen final comparison and every
   recorded qualifying structure's atomic economics from the minimal hit snapshot;
4. inspection confirms the product path did not persist normal non-hit full-market data or the
   theoretical structure universe.

A fully covered zero-hit interval is truthful and must not cause detector tuning, but it does not
establish hit reachability. An unavailable interval is `UNKNOWN`, not zero. The service may keep
running until a hit occurs or a human stops it; no predetermined duration can force acceptance.

This closure requires no Candidate, Shadow Entry, Position action, mature Outcome, profitability,
full-market archive, or reconstruction of the rolling forecast from persisted raw events.

## Queued sequence — not authorized

After Radar establishment is explicitly accepted and this document is advanced:

1. **Underwriting, Shadow admission, and Position contract:** freeze
   `CANDIDATE | WATCH | ABSTAIN`, `HOLD | CLOSE | UNKNOWN`, hard-close priority, and exact atomic
   versus legged close semantics before admitting any Shadow Entry.
2. **Fixed-Policy forward cohort:** record complete Radar, Candidate, Shadow Entry, Shadow close,
   Shadow Outcome, rejected-counterfactual, and cohort-aligned `NO_TRADE` denominators.
3. **Challenger research and qualification:** authorize only after a usable frozen-Policy cohort
   and pre-registered qualification contract.
4. **Promotion and execution:** authorize separately after independent qualification. Account,
   capital, order, and fill authority remain distinct.

A queued closure is not active. Exactly one may be activated only by an explicitly approved
change after its prerequisite is accepted.

## Forbidden under the current boundary

- changing the detector after seeing production acceptance results;
- calling a pointwise IV indicator model-free VRP or a proven edge;
- attaching an anomaly to a structure whose short leg is outside the triggered scope;
- treating legged component quotes as an atomic structure or any public quote as a fill;
- full-market persistence, per-update no-hit receipts, or replay as the production scanner;
- Candidate, Shadow admission, executed entry, Position runtime, or Outcome cohort behavior;
- scheduled-event, portfolio, account, or capital gates;
- learned models, automated research, qualification, promotion, or evolution;
- generic databases, feature stores, registries, workflow engines, services, markets, or
  strategies;
- private/test/account APIs, credentials, balances, margin, positions, orders, fills, settlement,
  execution gateways, or money.

Update this document in the same accepted change that changes permission, implemented capability,
blockers, or the sole authorized next closure.
