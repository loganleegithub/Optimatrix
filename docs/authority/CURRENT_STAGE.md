# Optimatrix Current Stage

**Status:** ACTIVE PERMISSION AUTHORITY

**Current permission boundary:** `PUBLIC_SHADOW`

**Current task kind:** `IMPLEMENTATION`

**Current implementation status:** `INVERSE_BTC_SHORT_VOL_V2_CONTROL_OUTCOME_PROJECTION_FIX_ACTIVE`

**Accepted online product:** `INVERSE_BTC_V1_ONLY`

**Accepted implementation boundary:** `INVERSE_BTC_SHORT_VOL_V2_PUBLIC_SHADOW`

**Persistent service:** `RUNNING_8765_CODE_DEB20E4F2922C823CDD96EA9B4150A6A2883FFAF_PENDING_ONE_CORRECTIVE_CUTOVER`

**Live commands:** `ONE_CLEAN_STOP_ONE_REPLACEMENT_START_AND_BOUNDED_API_BROWSER_GATE_AUTHORIZED`

**Sole authorized closure:**
[`SHADOW_SCHEME_TWO_DEPLOYMENT`](../../tasks/SHADOW_SCHEME_TWO_DEPLOYMENT.md)

## Accepted live baseline

PR `#53` merged the scheme-two Shadow trader surface. PRs `#54` and `#55` authorized and completed
the stable launch. The sole runtime at `127.0.0.1:8765` runs code identity
`deb20e4f2922c823cdd96ea9b4150a6a2883ffaf` from the clean deployment checkout at
`/Users/logan/Optimatrix-runtime`, with runtime identity
`sha256:9ef0082cf3f860247ea8409416cebe3a04f8ba13a9d91b9693258e8fd5ce22af`.

The accepted API baseline is schema 7, `RUNNING / CURRENT / ready`, `KNOWN_COMPLETE 130 / 130`,
zero reconnects, zero session gaps, 20 admitted Shadow rows, and no Controls in the trader Shadow
book. Desktop and 800px browser observations match scheme two, preserve the public-read-only
boundary, keep the 1000px risk table inside its own scroller, avoid document-level horizontal
overflow, and report no application console warning or error.

The stable Case root remains
`/Users/logan/OptiMatrix_DATA/Deribit/optimatrix-shadow-v2-v9`. The runtime recovered 33 compatible
responsibilities. Public facts naturally completed eight of them after recovery: one
`MARKET_EXIT` and seven `CONTRACT_SETTLEMENT` Outcomes. The official active reader accepts all 92
schema-v5 Cases and now derives 44/44 known terminal-economic Cases, 23/23 strict terminal-sample
Cases, and 25 pending responsibilities. No Case was migrated, relabeled, backfilled, or rewritten.

## Proven projection contradiction

The bounded current-state store intentionally retains the latest terminal Case beside active
Cases. The latest terminal Case is Radar score-band Control
`sha256:d4bfb163f15a7b69a576dd4aa54af74a82118d6d087d9773d03fdf480a26fafe`.
Its Position projection correctly says `TERMINAL / SETTLED_KNOWN` and the official reader excludes
it from the 25 active responsibilities. Its Outcome projection incorrectly says `PENDING` because
the Workbench joins only `SHADOW_OUTCOME`, despite the matching
`RADAR_SCORE_BAND_NO_TRADE_CONTROL_OUTCOME` already being present. The same omission affects the
selected underwriting Control Outcome kind.

## Authorized corrective cutover

This closure authorizes exactly:

1. make Position and Outcome projections consume one shared set containing `SHADOW_OUTCOME`,
   `SELECTED_UNDERWRITING_DECISION_CONTROL_OUTCOME`, and
   `RADAR_SCORE_BAND_NO_TRADE_CONTROL_OUTCOME`, with direct tests for both Control kinds;
2. merge the tested implementation through Draft PR `#56`;
3. clean-stop runtime
   `sha256:9ef0082cf3f860247ea8409416cebe3a04f8ba13a9d91b9693258e8fd5ce22af`,
   synchronize the clean deployment checkout to merged `main`, and start exactly one replacement
   against the unchanged stable Case root and port;
4. verify import provenance, code/runtime identity, health/readiness, schema-7 API, the official
   Case reader, the retained terminal Control Outcome, and the admitted-only scheme-two browser
   surface.

Normal stop/start may append only contract-authorized Segment facts and naturally observed
terminal Outcomes. No historical Case may be migrated, copied, deleted, relabeled, rewritten, or
backfilled. Qualification remains offline and window-specific.

## Product truth and non-claims

The sole Online Runtime product remains `INVERSE_BTC_V1` under the unchanged fixed Radar,
Underwriting, and Position Policy identities. The current Position Policy identity and all nine
frozen predicate thresholds remain unchanged. No product, schema family, Case root, durable record
kind, market source, order/fill/account boundary, or private permission changed.

Permission remains `PUBLIC_SHADOW`: production public facts and counterfactual economics only. A
public book or delivery price is not an order, fill, settlement action, actual position, account
PnL, or capital exposure. `PENDING`, `GAPPED`, a responsive Workbench, green tests, or a recovered
Case do not qualify a Policy or prove terminal economics. This task authorizes no order, fill,
account, capital, private-data, second-product, second-Case-root, Policy, or qualification change.
