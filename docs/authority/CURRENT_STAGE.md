# Optimatrix Current Stage

**Status:** ACTIVE PERMISSION AUTHORITY

**Current permission boundary:** `PUBLIC_SHADOW`

**Current implementation status:** `INVERSE_BTC_PRODUCT_CONSTRUCTION_AUTHORIZED`

**Accepted implementation boundary:** `LINEAR_BTC_USDC_SELECTED_DECISION_OUTCOME_OBSERVED`

**Production Short Vol Radar:** `LINEAR_BTC_USDC_V1_ACCEPTED`

**Persistent service:** `STOPPED_CLEANLY`

**Live commands:** `FORBIDDEN`

**Sole authorized closure:** `SHORT_VOL_INVERSE_BTC_PRODUCT_CONTRACT_AND_NORMALIZATION`

## Current truth

The accepted production-public component-book lifecycle remains a counterfactual. It is
`NOT_AN_ORDER`, `NOT_A_FILL`, `NOT_AN_ATOMIC_QUOTE`, provides no liquidity reservation, and proves
neither fillability nor strategy edge.

The final repaired Linear BTC-USDC process used code identity
`6dee819961d76b622dbc6b77997e1f987451a096`, runtime identity
`sha256:fdb4f0b3eadfc0f892cfad210142d14c521394cfeb6fbd5c761554228c45998f`, and state root
`/private/tmp/optimatrix-natural-shadow-currentness-repair-T6MhNA`. It stopped cleanly at explicit
human request with exit status `0` and is not authorized to restart. Its terminal canonical funnel
was:

```text
APPLICABLE_MARKET_SCOPE                         5,043,177 contract evaluations
RADAR_KNOWN                                    5,040,616 contract evaluations
ANOMALY_ACTIVE                                        11 distinct Episodes
STRUCTURE_REVIEWABLE                                   6 distinct Episodes
COMPONENT_BOOK_COUNTERFACTUAL_EVALUABLE                6 distinct Episodes
UNDERWRITING_EVALUABLE                                 6 distinct Episodes
CANDIDATE                                               0 distinct Episodes
SHADOW_CASE_OPENED                                      0 admitted-Candidate Cases
SHADOW_CASE_OUTCOME                                     0 admitted-Candidate Outcomes
```

The `2,561` post-warmup Radar-knownness gap conserves as `OPTION_BOOK_UNKNOWN 1,792`,
`POST_STATUS_BOOTSTRAP_REQUIRED 768`, and `NUMERICAL_BOUNDARY_UNRESOLVED 1`. Conditional on reaching
Underwriting, all six structures stopped at `CREDIT_NOT_ABOVE_FUTURE_COST_RESERVE 6`. These are
measured Linear results; they do not predict Inverse BTC opportunity frequency or economics.

The separate selected-decision research projection made six future-blind selections, all original
`ABSTAIN`. Five received an eligible strictly later refresh and opened no-trade control Cases. One
terminalized as `UNKNOWN_CONSUMED` because its local receive skew was `7,100 ms`, above the frozen
`4,000 ms` budget. Each of the five Case directories contains `opened.json`, `first-close.json`,
and `outcome.json`; the official reader reports `COMPLETE`. Every terminal Outcome is
`CENSORED_AT_STOP`, with economic availability `UNKNOWN` and no PnL. `COMPLETE` therefore means
lifecycle-terminal, not known economics, profitability, or Policy quality.

## Authorized product change

The next product closure constructs `INVERSE_BTC_V1` beside the accepted `LINEAR_BTC_USDC_V1`
inside the same modular monolith. Product identity must bind market family, quote and settlement
currencies, price index, native option-price unit, model-normalization rule, valuation-conversion
rule, fee rule, and durable Case schema. Similar Deribit instrument parsing does not authorize
Linear economics to be reused for Inverse.

The implementation may share transport, the one causal reducer, Radar/Underwriting/Position state
machines, the one component-book calculator, and the bounded Case store. It must keep the two
three-Policy chains content-distinct, reject cross-product legs and Policies, preserve Linear schema
v3 byte semantics, and use product-aware schema v4 only for a future Inverse Case.

Inverse construction is USD-defined risk with native BTC premium, fees, settlement cashflow, and
liability. The current USD valuation is an explicit conversion at a declared BTC index boundary;
it is not a native cashflow and is not an account-margin fact. Public product metadata and books do
not provide actual account margin, which remains `UNKNOWN`.

## Allowed work

- modify the bounded product/domain/runtime/Workbench/Case surfaces and their owning contracts for
  strict `LINEAR_BTC_USDC_V1 | INVERSE_BTC_V1` selection;
- add separate Inverse Radar, Underwriting, and Position Policy artifacts without changing the
  accepted Linear Policy bytes or decisions;
- run deterministic offline formula, parser, state-machine, persistence, Workbench, compatibility,
  and full repository checks;
- read the five existing Linear v3 Case directories through the product-aware official reader.

## Forbidden work

- any Deribit probe, smoke, natural Shadow process, service start, restart, or simultaneous product
  observation in this implementation closure;
- any private/account API call, credential, balance, margin, order, fill, capital, settlement
  action, supervised deployment, commissioning, or actual exposure;
- changing accepted Linear thresholds, target quantity, universe, reserve thresholds, fee
  semantics, or Case v3 bytes;
- inferring Inverse frequency, liquidity acceptance, Candidate conversion, Outcome quality,
  profitability, qualification, or deployment readiness from code, fixtures, volume/open interest,
  or green checks;
- a second runtime stack, reducer, calculator, schema validator, database, replay platform,
  full-feed persistence, supervisor, host inspection, or operational-control subsystem.

## Acceptance boundary

This closure is accepted only when one strict startup product selection reaches the existing shared
state-machine path in deterministic fixtures; product identities and Policy chains fail closed on
mixing; native, model, valuation, fee, payoff, entry, close, and Outcome arithmetic conserve in
their declared units; Linear v3 behavior and existing records remain compatible; trader-facing
state names the product and units; focused tests and `make check` pass; and the diff adds no second
business owner.

Acceptance authorizes construction only. Runtime observation topology, state roots, exact code
identity, and whether separate single-product observations run sequentially or concurrently require
a later explicit `VALIDATION_ONLY` task after merge. One process still binds exactly one product.
Until then the sole registered live topology is `NONE`.
