# Optimatrix Current Stage

**Status:** ACTIVE PERMISSION AUTHORITY

**Current permission boundary:** `PUBLIC_SHADOW`

**Current task kind:** `IMPLEMENTATION`

**Current implementation status:** `INVERSE_BTC_SHORT_VOL_V2_STRONG_SIGNAL_MAP_CUTOVER`

**Accepted online product:** `INVERSE_BTC_V1_ONLY`

**Accepted implementation boundary:** `INVERSE_BTC_SHORT_VOL_V2_PUBLIC_SHADOW`

**Persistent service:** `STOPPED_UNCLEAN_8675_WITH_32_RECOVERABLE_ADMITTED_ENTRIES`

**Live commands:** `ONE_CLEAN_START_8765`

**Sole authorized closure:**
[`INVERSE_BTC_SHORT_VOL_V2_STRONG_SIGNAL_MAP`](../../tasks/INVERSE_BTC_SHORT_VOL_V2_STRONG_SIGNAL_MAP.md)

## Current bounded implementation authority

PR #48 is merged as `main@d237aaf4579ab041441edebae93a4c56f32031c4`, and the strong-signal
map is merged by PR #47 as `main@54e3b589ba5ffd7eff7f7acf018fbc0530492614`. The source-grid,
confirmation-continuity, Decimal, Candidate-retirement, heartbeat, and typed activation-packet
repairs are accepted implementation and must remain in the deployment code identity. The prior
repair task is complete and remains only in Git and PR history.

The one active task now performs the user-requested canonical Workbench cutover from the already
stopped port `8675` to port `8765`. The bounded pre-start probe found no service on 8675. The one
official inactive-runtime Case inventory validated 47 schema-v5 Cases: 32 admitted Shadow Entries,
4 selected Underwriting Controls, and 11 Radar score Controls. All 47 retain
`INCOMPLETE_UNCLEAN_EXIT`, with zero mature Outcome. The browser may select only server-owned
eligible `HIGH` bucket leaders in `CONFIRMING | ACTIVE`, report the visible count against the
complete current Radar denominator, retain expiry/strike topology, and expose the selected server
evidence. It may not calculate score, leader, confirmation, forward, history, Candidate, structure,
or Shadow truth.

After focused and repository gates, this task authorizes exactly:

1. synchronization of the non-temporary `/Users/logan/Optimatrix-runtime` checkout to the exact
   clean `main@54e3b589ba5ffd7eff7f7acf018fbc0530492614`;
2. one clean start on `127.0.0.1:8765` with the unchanged stable Case repository
   `/Users/logan/OptiMatrix_DATA/Deribit/optimatrix-shadow-v2-v9` and the unchanged three-Policy
   chain; and
3. one bounded post-start GET/HEAD verification of all six routes, schema/product/Policy identity,
   `RUNNING/CURRENT/ready`, complete Radar coverage, the strong-signal map assets, and restoration
   of every compatible non-terminal admitted Entry.

No stop command remains: the prior process already ended without closing its latest Segments. The
one new runtime must restore all 32 admitted Entry aggregates and open truthful `HANDOFF_GAP`
Segments for them; Controls are not restored. No Case,
Entry, Control, or Outcome may be copied, migrated, rewritten, deleted, or relabeled. Port `8675`
must no longer serve after cutover, and no second Runtime may run concurrently. A failed or consumed
start grants no retry. Policy/schema changes, roadmap-channel work, private execution, host
inspection, commissioning machinery, and deployment supervision remain forbidden.

## Current online boundary

The last accepted Online Runtime on `127.0.0.1:8675` used clean PR #48 code identity
`2739ad26745aca2884ed56f296b8d4d3d07ff9cc` and runtime identity
`sha256:9a6d7f937d08118eb13c15e4dd511d67c8b1c8232b2c69560bf8d402fb377688`
from `/Users/logan/Optimatrix-runtime`. It is no longer serving and no process holds the stable-root
lease. The inactive official reader validates all 47 existing Cases without mutation; 32 admitted
Entries are non-terminal and therefore mandatory recovery inputs for the next runtime. Their latest
Segments are `INCOMPLETE_UNCLEAN_EXIT`, so the next observation quality is permanently GAPPED. The
four Controls and eleven Radar Controls remain historical Case truth and are not restored.

## Current product truth

The sole Online Runtime product is `INVERSE_BTC_V1`. Its channel is
`INVERSE_BTC_SHORT_VOL_V2`, Workbench document schema is `7`, and durable Shadow Case family is
schema v5. It consumes Deribit production public BTC options and `btc_usd`, uses BTC-native
premium/fees/settlement/PnL, and labels current valuation as `USD_EQUIVALENT`.

There is no product selector, fallback product, compatibility profile, alternate online schema, or
in-process Policy switch. The repository contains only the three fixed V2 Inverse Policy artifacts:

```text
product spec identity:        sha256:a7880d3a0b3da12f74438b292ed49d7c034e683d2e1654037229c62474127131
Radar Policy identity:        sha256:fd604c22b6f4a111955f432fe09647e93c38e914e81c4045905ca79b935bdc9d
Underwriting Policy identity: sha256:933dce3e4d9736b465aaca95a352ef8c3196592bfef04cf1f958442afe0f5e7d
Position Policy identity:     sha256:8a00bacc13f5f3f2407ea3ff5060464e12d93c3f336f9d1f9d750a0621fa0ffe
```

The V2 score is an expert ordinal opportunity-ranking hypothesis, not a probability, oracle,
expected return, Edge, or profitability claim. The component-book lifecycle is a public-book
counterfactual, not an order, fill, atomic quote, liquidity reservation, or actual position.

## Permission and non-claims

Permission remains `PUBLIC_SHADOW`: no credential, account, balance, margin, order, fill, capital,
settlement action, actual exposure, or private execution. Green checks and a responsive Workbench
do not establish future uptime, source freshness, fillability, qualification, Edge, or
profitability. After the declared one-start and bounded verification are consumed, any
additional live probe, restart, state-root operation, Policy change, or roadmap-channel
implementation requires a new explicit task and permission update.
